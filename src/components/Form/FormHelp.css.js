import styled from 'styled-components'
import { colors, fonts, layout } from '../../styles'

export const StyledFormHelp = styled.div`
    font-size: ${fonts.size.small};
    color: rgb(${colors.grey});
    margin-top: calc(${layout.spacer} / 4);
`
